{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "${author}"
  ],
  "contact": {
    "homepage": "${websiteUrl}",
    "sources": "${sourcesUrl}",
    "issues": "${issueTrackerUrl}"
  },
  "license": "${license}",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "net.cerealcamera.aviator_dream.fabric.AviatorDreamsFabric"
    ],
    "client": [
      "net.cerealcamera.aviator_dream.fabric.AviatorDreamsFabricClient"
    ]
  },
  "mixins": [
    "aviator_dream.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.11.3",
    "minecraft": ">=1.21.1",
    "immersive_aircraft": ">=1.3.0",
    "fabric-api-base": ">=0.1.0"
  }
}
